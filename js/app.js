/* ============================================================
   APP — Main entry point
   Portfolio / js/app.js
   ============================================================ */

"use strict";

/**
 * Application bootstrap.
 * Called after all deferred scripts are loaded and DOM is ready.
 */
document.addEventListener("DOMContentLoaded", () => {

    console.info("[app] Portfolio initialised.");

    /*
     * Future component loading calls go here, e.g.:
     *
     * import { loadComponent } from "./components.js";
     * loadComponent("navbar.html",  "header");
     * loadComponent("footer.html",  "footer");
     * loadComponent("featured.html","#featured");
     */

});
